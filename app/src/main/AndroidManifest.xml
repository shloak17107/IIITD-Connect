<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iiitdconnect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iiitd_connect_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iiitd_connect_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".showPostActivity">

        </activity>
        <activity android:name=".UserType" />
        <activity android:name=".CreatePost" />
        <activity android:name=".Profile"/>
        <activity android:name=".Profilechange" />
        <activity android:name=".RegistrationActivity" android:screenOrientation="portrait"/>
        <activity android:name=".LoginActivity" android:screenOrientation="portrait"/>
        <activity android:name=".profile_see" />
        <activity android:name=".profile_enter" />
        <activity android:name=".Feed" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-feature android:name="android.hardware.camera2" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>